#include <stdio.h>

/**
 * main - Prints the number of paramters
 * @argc: Number of command line arguments
 * @argv: Array name
 * Return: 0
 */

int main(int argc, char *argv[])
{
	(void) argv;
	printf("%d\n", argc);

	return (0);
}
